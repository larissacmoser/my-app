import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#777777",
    },
    secondary: {
      light: "#F21B62",
      main: "#64BD8A",
    },
  },
});

export default theme;
